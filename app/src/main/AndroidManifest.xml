<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.woongsnote.mcb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mcb">
        <activity
            android:name=".pages.user.UserActivity"
            android:exported="false" />
        <activity
            android:name=".pages.detail.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".pages.search.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".pages.user.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".pages.user.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"/>
    </application>

</manifest>